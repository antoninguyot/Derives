import React from "react";
import {Text, View} from "react-native";

const AudioPage = () => {
  return(
    <View style={{flex:1}}>
      <Text>Mode Audio (WIP)</Text>
    </View>
  );
};

export default AudioPage;
